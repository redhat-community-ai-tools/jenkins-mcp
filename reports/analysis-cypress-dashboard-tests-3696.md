# Jenkins Job Analysis: cypress/dashboard-tests

**Build:** [3696](https://jenkins-csb-rhods-opendatascience.dno.corp.redhat.com/job/cypress/job/dashboard-tests/3696/)
**Date:** 2025-12-10 09:30:46

## Test Results

- Total: 91
- Passed: 27
- Failed: 64
- Skipped: 0

## Failed Tests

### 1. Verify Cluster Storage - Creating, Editing and Deleting Verify Cluster Storage - Creating, Editing and Deleting Create, Edit and Delete a Persistent Volume Storage

**File:** `cypress/tests/e2e/storage/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 2. Verify the filters on Resources page Verify the filters on Resources page Test whether enabled, resource type, provider and provider type filters are working

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 3. Verify Connections - Creation and Deletion Verify Connections - Creation and Deletion Create and Delete a Connection

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 4. Workbenches - variable tests Workbenches - variable tests Verify user can set environment variables in their workbenches by uploading a yaml Secret and Config Map file.

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 5. Workbenches - variable tests Workbenches - variable tests Verify that the user can inject environment variables manually into a workbench using Key / Value

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 6. Verify NIM enable flow Verify NIM enable flow Enable and validate NIM flow

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Login to the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 7. Verify Workload Metrics Default page Contents Verify Workload Metrics Default page Contents Verify Workload Metrics Home page Contents

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Check if Kueue is set to Unmanaged in the DSC 2. Login to the Application 3. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` ins
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 8. Verify Workload Metrics Default page Contents Verify Workload Metrics Default page Contents Verify Project Metrics Default Page contents

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Check if Kueue is set to Unmanaged in the DSC 2. Login to the Application 3. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` ins
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 9. Verify Workload Metrics Default page Contents Verify Workload Metrics Default page Contents Verify Distributed Workload status Default Page contents

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Check if Kueue is set to Unmanaged in the DSC 2. Login to the Application 3. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` ins
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 10. Workbenches - image/version tests Workbenches - image/version tests Verifies that workbench images have an additional dropdown which supports N/N-1 image versions.

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 11. Workbenches - negative tests Workbenches - negative tests Verify UI informs users about workbenches failed to start

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 12. Workbenches - negative tests Workbenches - negative tests Verify User cannot create a workbench using special characters or long names in the Resource name field

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 13. Workbenches - status tests Workbenches - status tests Verify user can access progress and event log - validate status and successful workbench creation

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 14. Verify that logged in users display on the Dashboard Verify that logged in users display on the Dashboard Verify that Admin Users can login and that login information displays on the dashboard

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 15. Verify that logged in users display on the Dashboard Verify that logged in users display on the Dashboard Verify that Non-Admin Users can login and that login information displays on the dashboard

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 16. Workbenches - tolerations tests Workbenches - tolerations tests Verify Workbench Creation using Hardware Profiles and applying Tolerations

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 17. Workbenches - tolerations tests Workbenches - tolerations tests Validate pod tolerations for a stopped workbench

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 18. Workbenches - tolerations tests Workbenches - tolerations tests Validate pod tolerations when a workbench is restarted with tolerations and tolerations are disabled

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 19. Start, Stop, Launch and Delete a Workbench in RHOAI Start, Stop, Launch and Delete a Workbench in RHOAI Starting, Stopping, Launching and Deleting a Workbench

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 20. Start, Stop, Launch and Delete a Workbench in RHOAI Start, Stop, Launch and Delete a Workbench in RHOAI Verify that a Workbench can be started and stopped using the Event log controls

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 21. Verify that admin users can edit a model registry Verify that admin users can edit a model registry "before all" hook for "Logs in as admin user and edits an existing model registry"

**File:** `cypress/tests/e2e/modelRegistry/unknown.cy.ts`

**Category:** timeout

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-438929' for model registry 5. Wait for model registry database 'model-registry-db-438929' to be ready 6. Create a model registry and verify it is ready 7. Create a model registry using YAML with database 'model-registry-db-438929' 8. Verify model registry is created 9. Wait for model registry to be in Ava
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 22. Verify that admin users can edit a model registry Verify that admin users can edit a model registry "after all" hook for "Logs in as admin user and edits an existing model registry"

**File:** `cypress/tests/e2e//cypress/tests/e2e/modelRegistry/testAdminEditRegistry.cy.ts`

**Category:** auth

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-438929' for model registry 5. Wait for model registry database 'model-registry-db-438929' to be ready 6. Create a model registry and verify it is ready 7. Create a model registry using YAML with database 'model-registry-db-438929' 8. Verify model registry is created 9. Wait for model registry to be in Ava
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 23. Verify models can be deployed from model registry Verify models can be deployed from model registry "before each" hook: retryableBeforeEach for "Registers a model and deploys it via model registry"

**File:** `cypress/tests/e2e/modelRegistry/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-128070' for model registry 5. Wait for model registry database 'model-registry-db-128070' to be ready 6. Create a model registry and verify it is ready 7. Create a model registry using YAML with database 'model-registry-db-128070' 8. Verify model registry is created 9. Wait for model registry to be in Ava
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 24. Verify models can be deployed from model registry Verify models can be deployed from model registry "after all" hook for "Registers a model and deploys it via model registry"

**File:** `cypress/tests/e2e//cypress/tests/e2e/modelRegistry/testRegistryDeployModel.cy.ts`

**Category:** auth

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-128070' for model registry 5. Wait for model registry database 'model-registry-db-128070' to be ready 6. Create a model registry and verify it is ready 7. Create a model registry using YAML with database 'model-registry-db-128070' 8. Verify model registry is created 9. Wait for model registry to be in Ava
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 25. Verify model registry permissions can be managed Verify model registry permissions can be managed "before all" hook for "Admin can add user permissions to model registry"

**File:** `cypress/tests/e2e//cypress/tests/e2e/modelRegistry/testManageRegistryPermissions.cy.ts`

**Category:** timeout

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-429402' for model registry 5. Wait for model registry database 'model-registry-db-429402' to be ready 6. Create a model registry using YAML 7. Verify model registry is created 8. Wait for model registry to be in Available state  `cy.exec('oc wait --for=condition=Available modelregistry.modelregistry.opend
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 26. Verify model registry permissions can be managed Verify model registry permissions can be managed "after all" hook for "Admin can add user permissions to model registry"

**File:** `cypress/tests/e2e//cypress/tests/e2e/modelRegistry/testManageRegistryPermissions.cy.ts`

**Category:** auth

**Error:**
```
Test steps were:  1. Load test data from fixture 2. Ensure operator has optimal memory for testing 3. Create and verify SQL database for model registry 4. Create SQL database 'model-registry-db-429402' for model registry 5. Wait for model registry database 'model-registry-db-429402' to be ready 6. Create a model registry using YAML 7. Verify model registry is created 8. Wait for model registry to be in Available state 9. Navigate away from model registry before cleanup  `cy.visit()` failed tryin
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 27. Verify Project - Creation and Deletion Verify Project - Creation and Deletion Create and Delete a Project in RHOAI

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 28. Verify Project - Creation and Deletion Verify Project - Creation and Deletion Verify users cannot create a project with Empty title

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 29. Verify Project - Creation and Deletion Verify Project - Creation and Deletion Verify User cannot create a project using special characters or long names in the Resource name field

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 30. Verify Project - Creation and Deletion Verify Project - Creation and Deletion Verify 250 character limit is enforced for Name and Description fields

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 31. Regular Users can make use of the Storage Classes in the Cluster Storage tab from Pipelines  Regular Users can make use of the Storage Classes in the Cluster Storage tab from Pipelines  If all SC are disabled except one, the SC dropdown should be disabled

**File:** `cypress/tests/e2e/pipelines/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automatically get 
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 32. Verify that users can provide contributor project permissions to non-admin users Verify that users can provide contributor project permissions to non-admin users "before each" hook: retryableBeforeEach for "Verify that user can be added as a Contributor for a Project"

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Command failed with code 1  Because this error occurred during a `before each` hook we are skipping the remaining tests in the current suite: `Verify that users can provi...`
Error: Command failed with code 1

Because this error occurred during a `before each` hook we are skipping the remaining tests in the current suite: `Verify that users can provi...`
    at Context.eval (webpack://@odh-dashboard/cypress/./cypress/utils/oc_commands/project.ts:58:0)
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 33. Cluster Storage Access Modes Tests Cluster Storage Access Modes Tests "before each" hook for "Should display storage classes with different access modes in cluster storage dropdown"

**File:** `cypress/tests/e2e/storage/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Provisioning storage classes with different access modes 2. Provisioning project 3. Log into the application 4. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/htm
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 34. Verify Admins Can Import and Delete a Custom Single-Model Serving Runtime Template By Uploading A YAML file Verify Admins Can Import and Delete a Custom Single-Model Serving Runtime Template By Uploading A YAML file Admin should access serving runtimes, import a yaml file and then delete

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 35. Verify OOTB Connection Types Verify OOTB Connection Types should have pre-installed label and unable to be edited or deleted

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to Connection Types view 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/settings/environment-setup/connection-types  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/settings/environment-setup/connection-types  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 36. Verify that only the Cluster Admin can access Cluster Settings Verify that only the Cluster Admin can access Cluster Settings Admin should access Cluster Settings and see UI fields matching OpenShift configurations

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 37. Verify that only the Cluster Admin can access Cluster Settings Verify that only the Cluster Admin can access Cluster Settings Test User - should not have access rights to view the Cluster Settings tab

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 38. application application should login and load page

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automatically get and set cookies 
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 39. Verify Project - Editing Verify Project - Editing Edit a Project in RHOAI

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 40. Create a custom resource Quickstart by using Dashboard CRDs Create a custom resource Quickstart by using Dashboard CRDs Upload custom resource and verify

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 41. An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can enable a disabled Storage Class

**File:** `cypress/tests/e2e/storage/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to Storage Classes view 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` wil
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 42. An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can disable an enabled Storage Class

**File:** `cypress/tests/e2e/storage/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to Storage Classes view 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` wil
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 43. An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can manage Storage Classes from Settings -> Storage classes view An admin user can set an enabled Storage Class as the default one

**File:** `cypress/tests/e2e/storage/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Navigate to Storage Classes view 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` wil
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 44. Verify Admin Single Model Creation and Validation using the UI Verify Admin Single Model Creation and Validation using the UI Verify that an Admin can Serve, Query a Single Model using both the UI and External links

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with htpasswd-cluster-admin-user 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visi
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 45. Verify Model Creation and Validation using the UI Verify Model Creation and Validation using the UI Verify that a Non Admin can Serve and Query a Model using the UI

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with ldap-user2 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.reques
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 46. A user can create an OCI connection and deploy a model with it A user can create an OCI connection and deploy a model with it Verify User Can Create an OCI Connection in DS Connections Page And Deploy the Model

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** resource

**Error:**
```
Test steps were:  1. Navigate to DS Project test-oci-deployment-889014 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 47. A model can be deployed with token auth A model can be deployed with token auth Verify that a model can be deployed with token auth

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with htpasswd-cluster-admin-user 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visi
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 48. ModelServing - tolerations tests ModelServing - tolerations tests Verify Model Serving Creation using Hardware Profiles and applying Tolerations

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with ldap-user2 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.reques
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 49. A model can be stopped and started A model can be stopped and started Verify that a model can be stopped and started

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with htpasswd-cluster-admin-user 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visi
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 50. Verify the RHOAI Application and Switcher links in the Dashboard Navigation Verify the RHOAI Application and Switcher links in the Dashboard Navigation Verify the various application tabs and switcher links are operational

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 51. Verify a Jupyter Notebook can be launched directly from the Project List View Verify a Jupyter Notebook can be launched directly from the Project List View Verify User Can Access Jupyter Launcher From Project Page

**File:** `cypress/tests/e2e/notebooks/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 52. Verify RHODS Explore Section Contains Only Expected ISVs Verify RHODS Explore Section Contains Only Expected ISVs Validate that configured ISVs display in the Explore Section

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Login to the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 53. Verify Notebook Server Administration Verify Notebook Server Administration Verify Admin User Can Access Notebook Administration

**File:** `cypress/tests/e2e/notebooks/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 54. Verify Notebook Server Administration Verify Notebook Server Administration Verify Non-Admin User cannot Access Notebook Administration

**File:** `cypress/tests/e2e/notebooks/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 55. Notebooks - tolerations tests Notebooks - tolerations tests Verify Juypter Notebook Creation using Hardware Profiles and applying Tolerations

**File:** `cypress/tests/e2e/notebooks/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 56. Verify Hardware Profiles - Creating, Editing and Deleting Verify Hardware Profiles - Creating, Editing and Deleting Create, Edit and Delete a Hardware Profile

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 57. Workbench Storage Classes Tests Workbench Storage Classes Tests "before each" hook for "Create workbench with RWO storage and verify storage attachment"

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Provisioning storage class 2. Provisioning project 3. Log into the application 4. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/projects  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely u
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 58. Verifies that Model Catalog is available for different users Verifies that Model Catalog is available for different users Verifies that Model Catalog is available for an admin user

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Login as admin user 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automatical
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 59. Verifies that Model Catalog is available for different users Verifies that Model Catalog is available for different users Verifies that Model Catalog is available for a regular user

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Login as LDAP user 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automaticall
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 60. Workbench and PVSs tests Workbench and PVSs tests Verify users can create a workbench and connect an existent PersistentVolume

**File:** `cypress/tests/e2e/workbenches/unknown.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 61. Verify that users can provide admin project permissions to non-admin users/groups Verify that users can provide admin project permissions to non-admin users/groups Verify that user can be added as an Admin for a Project

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 62. Verify that users can provide admin project permissions to non-admin users/groups Verify that users can provide admin project permissions to non-admin users/groups Verify user can assign access permissions to user group

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automa
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 63. Verify that users can provide admin project permissions to non-admin users/groups Verify that users can provide admin project permissions to non-admin users/groups Verify that user can access the created project with Admin rights

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Log into the application with ldap-user2 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.reques
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent

### 64. Verify RHODS About Dialog Verify RHODS About Dialog Verify RHODS About Dialog contains correct information

**File:** `cypress/tests/e2e/modelRegistry/testArchiveModels.cy.ts`

**Category:** unknown

**Error:**
```
Test steps were:  1. Login to Open Data Hub 2. Navigate to: https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  `cy.visit()` failed trying to load:  https://data-science-gateway.apps.dash-e2e-odh.osp.rh-ods.com/  The `content-type` of the response we received from your web server was:    > `undefined`  This was considered a failure because responses must have `content-type: 'text/html'`  However, you can likely use `cy.request()` instead of `cy.visit()`.  `cy.request()` will automati
```

**✗ Rerun Result:** FAILED on rerun (exit code: 1)

> **Note:** Test consistently fails - not intermittent